# Project: KineTopus Engine Comparative Benchmark Environment (CBE)

## Architecture
The Comparative Benchmark Environment (CBE) provides standardized comparative evaluation for time-series forecasting across four model families:
1. **KineTopus Engine (SINDy)**: Physics-informed discovery engine (`WalkForwardEvaluator` in `src/quant_engine/evaluator.py`).
2. **AutoARIMA**: Statistical baseline (`AutoARIMAWalkForwardEvaluator` in `src/quant_engine/arima_evaluator.py`).
3. **Naive Persistence**: Zero-intelligence baseline predicting $P(t+k)=P(t)$ (`NaiveWalkForwardEvaluator` in `src/quant_engine/naive_evaluator.py`).
4. **LSTM Baseline**: Deep learning sequence baseline (`LSTMWalkForwardEvaluator` in `src/quant_engine/lstm_evaluator.py`).

All evaluators conform to a unified walk-forward interface and output a 247-column DataFrame with per-block metrics ($b \in [1, 60]$) and iteration metadata.

## Feature Inventory
| # | Feature | Description | Milestone | Source |
|---|---------|-------------|-----------|--------|
| 1 | Evaluator Base Contract | Duck-typed interface: `__init__(df, disable_norm, disable_returns)` and `.run(initial_window, stride, horizon, blocks)` returning 247-col DataFrame. | M1, M2 | Survey |
| 2 | Naive Persistence Evaluator | `NaiveWalkForwardEvaluator` in `src/quant_engine/naive_evaluator.py` predicting flat prices, setting `Validez='OK'`, identical `MAPE` and `Naive_MAPE`. | M1 | ORIGINAL_REQUEST R1 |
| 3 | LSTM Baseline Evaluator | `LSTMWalkForwardEvaluator` in `src/quant_engine/lstm_evaluator.py` using PyTorch, 1 layer (32 units), Z-score in-sample scaling, recursive forecasting, early stopping, fallback `FALLO MATEMÁTICO`. | M2 | ORIGINAL_REQUEST R2 |
| 4 | E2E Test Suite & Infra | Opaque-box E2E test suite (`tests/test_cbe_e2e.py`) validating requirements across Tiers 1-4, publishing `TEST_READY.md`. | E2E | Dual Track |
| 5 | Unified Benchmark Notebook | `notebooks_val/7_Comparative_Benchmark.ipynb` orchestrating evaluation on BTC-USD, MSFT, XLF with checkpointing to `unified_benchmark_db.csv` and Plotly/Matplotlib visual comparisons. | M3 | ORIGINAL_REQUEST R3 |
| 6 | Automated Sanity Test | `notebooks_val/test_benchmark_sanity.py` running mini Walk-Forward (MSFT, 5 iterations) with 6 automated checks. | M4 | ORIGINAL_REQUEST R4 |

## Code Layout
- `src/quant_engine/naive_evaluator.py`: Naive persistence evaluator module. Owned by M1.
- `src/quant_engine/lstm_evaluator.py`: LSTM baseline evaluator module. Owned by M2.
- `notebooks_val/7_Comparative_Benchmark.ipynb`: Comparative benchmark notebook. Owned by M3.
- `notebooks_val/test_benchmark_sanity.py`: Automated sanity test script. Owned by M4.
- `tests/test_cbe_e2e.py`: End-to-end requirement-driven test suite. Owned by E2E Track.
- `notebooks_val/unified_benchmark_db.csv`: Benchmark results database generated by notebook.

## Milestones
| # | Name | Scope | Dependencies | Status |
|---|------|-------|-------------|--------|
| E2E | E2E Test Suite | Create opaque-box E2E test harness and test cases in `tests/test_cbe_e2e.py` | none | IN_PROGRESS |
| M1 | Naive Persistence Evaluator | Implement `NaiveWalkForwardEvaluator` in `src/quant_engine/naive_evaluator.py` | none | DONE |
| M2 | LSTM Baseline Evaluator | Implement `LSTMWalkForwardEvaluator` in `src/quant_engine/lstm_evaluator.py` | none | DONE |
| M3 | Unified Benchmark Notebook | Implement `notebooks_val/7_Comparative_Benchmark.ipynb` with checkpointing & plotting | M1, M2 | IN_PROGRESS |
| M4 | Automated Sanity Test & Verification | Implement `notebooks_val/test_benchmark_sanity.py` and pass full E2E & sanity test suite | M1, M2, M3, E2E | PLANNED |

## Interface Contracts
### Evaluator Interface Contract
```python
class EvaluatorProtocol:
    def __init__(self, df: pd.DataFrame, disable_norm: bool = False, disable_returns: bool = False):
        ...
    def run(self, initial_window: int = 150, stride: int = 20, horizon: int = 300, blocks: int = 60) -> pd.DataFrame:
        ...
```

### Result Schema Contract (247 columns)
- Metadata: `['Model', 'Ticker', 'Iteracion (Velas Vistas)', 'Drift (k)', 'SINDy R2', 'Validez', 'Latencia_ms']`
- Per-block ($b \in [1, 60]$): `['MAPE_B{b}', 'Naive_MAPE_B{b}', 'Hit_B{b}', 'CumHit_B{b}']`
